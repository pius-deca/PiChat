server:
  port: ${PORT:8088}
  servlet:
    context-path: "/api/v1"

spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:db;DB_CLOSE_DELAY=-1
    username: sa
    password: sa
    # data:
    # - classpath:/data-test.sql

com:
  cloudinary:
    cloud_name: ${cloud_name:name}
    api_key: ${api_key:aasdfghytrf}
    api_secret: ${api_secret:Yhgfdstyuyikjhjuyt678iuj}
